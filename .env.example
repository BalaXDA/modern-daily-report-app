# Local dev: SQLite file (no install, zero setup).
# Vercel/prod: paste your Postgres URL here. The prebuild script automatically
# switches the Prisma provider to postgresql when DATABASE_URL starts with
# postgres:// or postgresql://.
DATABASE_URL="file:./dev.db"
